TITLE Password Validator
SAY Setup your password:
$saved_pwd LISTEN

SAY Password saved! Now please log in.
LOOP:
  SAY Enter your password:
  LISTEN
  IF the attempt above matches the saved password {$saved_pwd}:
    SAY Access Granted!
    EXIT LOOP
  ELSE:
    SAY Incorrect password. Try again.
